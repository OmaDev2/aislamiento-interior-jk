---
import { Image } from "astro:assets";
import heroImg from "../assets/images/hero-bg.png";
import business from "../data/business.json";
import home from "../data/home.json";

interface Props {
  h1: string;
  subtitle?: string;
}

const {
  h1 = home.h1 || "¿Paredes Frías o Humedad en Casa?",
  subtitle = home.heroSubtitle ||
    "Elimina el frío, el moho y la condensación en 48h sin reformar tu vivienda.",
} = Astro.props;

const phone = business.phone || "93 694 76 63";
const phoneLink = `tel:${phone.replace(/\s/g, "")}`;
---

<section
  class="hero-section relative py-20 lg:py-32 overflow-hidden bg-[#1E3A8A]"
>
  <!-- Astro Image Background -->
  <div class="absolute inset-0 z-0">
    <Image
      src={heroImg}
      alt="Aislamiento térmico interior de alta calidad en Barcelona"
      class="w-full h-full object-cover opacity-45"
      loading="eager"
    />
    <!-- Overlay for readability -->
    <div
      class="absolute inset-0 bg-[#1E3A8A]/85 mix-blend-multiply transition-opacity duration-300"
    >
    </div>
    <div
      class="absolute inset-0 bg-gradient-to-tr from-[#1E3A8A] via-transparent to-[#1E3A8A]/20 opacity-90"
    >
    </div>
  </div>

  <!-- Content Container -->
  <div class="container mx-auto px-4 relative z-10">
    <div class="flex flex-col lg:flex-row items-center gap-12 lg:gap-16">
      <!-- LEFT CONTENT: Text and Info -->
      <div class="w-full lg:w-3/5 text-white">
        <!-- Trust Badge ... (rest of the component remains the same) -->

  <!-- Content Container -->
  <div class="container mx-auto px-4 relative z-10">
    <div class="flex flex-col lg:flex-row items-center gap-12 lg:gap-16">
      <!-- LEFT CONTENT: Text and Info -->
      <div class="w-full lg:w-3/5 text-white">
        <!-- Trust Badge -->
        <div
          class="trust-badge flex items-center gap-2 bg-white/10 backdrop-blur-md border border-white/20 rounded-full px-4 py-1.5 mb-8 w-fit animate-fade-in-down"
        >
          <svg width="20" height="20" fill="#FFB800" viewBox="0 0 24 24">
            <path
              d="M12 17.27L18.18 21l-1.64-7.03L22 9.24l-7.19-.61L12 2 9.19 8.63 2 9.24l5.46 4.73L5.82 21z"
            ></path>
          </svg>
          <span class="text-xs md:text-sm font-medium tracking-tight">
            4.9/5 - 87 Valoraciones +80 viviendas en Barcelona
          </span>
        </div>

        <!-- H1 Heading -->
        <h1
          class="text-4xl md:text-5xl lg:text-6xl font-extrabold leading-tight mb-6 animate-fade-in text-white"
        >
          {h1}
          <br />
          <span class="text-[#F97316] block mt-1 drop-shadow-sm"
            >Solución Sin Obras en Barcelona</span
          >
        </h1>

        <!-- Subtitle -->
        <p
          class="text-lg md:text-xl text-white/90 mb-10 leading-relaxed max-w-2xl animate-fade-in-up"
        >
          {subtitle}
          <br class="hidden md:block" />
          Ahorra hasta un 40% en calefacción desde el primer invierno.
        </p>

        <!-- Pain Points Cards -->
        <div
          class="grid grid-cols-2 sm:grid-cols-4 gap-4 mb-10 animate-fade-in-up delay-200"
        >
          {
            [
              {
                icon: "M2 12h10M9 4v16m-6-11 3 3-3 3M12 6 9 9 6 6m0 12 3-3 1.5 1.5M20 4v10.54a4 4 0 1 1-4 0V4a2 2 0 0 1 4 0Z",
                text: "Paredes heladas",
              },
              {
                icon: "M8 2c3 0 5 2 8 2s4-1 4-1v11s-1 1-4 1-5-2-8-2-4 1-4 1V2s1-1 4-1M4 22v-7",
                text: "Moho y humedad",
              },
              {
                icon: "M12 1v22M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6",
                text: "Facturas altas",
              },
              {
                icon: "M4 2h16v20H4V2Zm5 20v-4h6v4M8 6h.01M16 6h.01M12 6h.01M12 10h.01M12 14h.01M16 10h.01M16 14h.01M8 10h.01M8 14h.01",
                text: "Sin SATE exterior",
              },
            ].map((item, i) => (
              <div class="group bg-white/5 backdrop-blur-sm border border-white/10 rounded-2xl p-4 text-center hover:bg-white/10 transition-all duration-300 hover:-translate-y-1">
                <div class="flex justify-center mb-3 text-white/80 group-hover:text-[#F97316] transition-colors">
                  <svg
                    width="24"
                    height="24"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                  >
                    <path d={item.icon} />
                  </svg>
                </div>
                <span class="text-[13px] font-semibold leading-tight">
                  {item.text}
                </span>
              </div>
            ))
          }
        </div>

        <!-- Benefits List -->
        <div
          class="flex flex-wrap gap-x-8 gap-y-4 mb-12 animate-fade-in-up delay-300"
        >
          {
            [
              "Sin tocar la fachada",
              "Garantía 10 años",
              "Sistema certificado",
              "Listo en 2-3 días",
            ].map((benefit) => (
              <div class="flex items-center gap-2">
                <svg width="20" height="20" fill="#10B981" viewBox="0 0 24 24">
                  <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z" />
                </svg>
                <span class="text-sm font-medium text-white/85">{benefit}</span>
              </div>
            ))
          }
        </div>

        <!-- Hero Buttons -->
        <div
          class="flex flex-col md:flex-row gap-4 items-center animate-fade-in-up delay-400 w-full"
        >
          <a
            href="#contacto"
            class="w-full md:w-auto px-8 py-5 bg-[#F97316] hover:bg-[#EA580C] text-white font-black text-lg rounded-full shadow-lg shadow-[#F97316]/40 transition-all text-center uppercase tracking-tight transform hover:scale-105 active:scale-95"
          >
            Presupuesto Gratis en 24h →
          </a>
          <a
            href={phoneLink}
            class="w-full md:w-auto px-8 py-5 bg-white text-[#F97316] hover:bg-gray-100 font-black text-lg rounded-full shadow-lg shadow-white/10 transition-all text-center flex items-center justify-center gap-2 uppercase tracking-tight transform hover:scale-105 active:scale-95"
          >
            <svg width="24" height="24" fill="currentColor" viewBox="0 0 24 24">
              <path
                d="M20.01 15.38c-1.23 0-2.42-.2-3.53-.56a.977.977 0 0 0-1.01.24l-1.57 1.97c-2.83-1.35-5.48-3.9-6.89-6.83l1.95-1.66c.27-.28.35-.67.24-1.02-.37-1.11-.56-2.3-.56-3.53 0-.54-.45-.99-.99-.99H4.19C3.65 3 3 3.24 3 3.99 3 13.28 10.73 21 20.01 21c.71 0 .99-.63.99-1.18v-3.45c0-.54-.45-.99-.99-.99z"
              ></path>
            </svg>
            O LLAMA AL {phone}
          </a>
        </div>
      </div>

      <!-- RIGHT FORM -->
      <div class="w-full lg:w-2/5 animate-fade-in-right">
        <div
          class="form-card bg-white rounded-3xl shadow-2xl p-8 md:p-10 text-gray-800"
        >
          <h2 class="text-3xl font-extrabold text-[#1E3A8A] mb-2">
            ¿Cuánto Me Cuesta?
          </h2>
          <p class="text-sm text-gray-500 mb-8 leading-relaxed font-medium">
            Te calculamos el precio en 24h · Inspección gratis a domicilio
          </p>

          <form
            id="hero-contact-form"
            name="hero-form"
            method="POST"
            action="/gracias/"
            data-netlify="true"
            class="space-y-4"
          >
            <input type="hidden" name="form-name" value="hero-form" />

            <div class="relative">
              <input
                type="text"
                name="nombre"
                placeholder="Tu nombre"
                class="w-full px-5 py-4 bg-gray-50 border border-gray-200 rounded-2xl focus:ring-2 focus:ring-[#1E3A8A] focus:border-transparent outline-none transition-all"
                required
              />
            </div>

            <div class="relative">
              <input
                type="tel"
                name="telefono"
                placeholder="Teléfono"
                class="w-full px-5 py-4 bg-gray-50 border border-gray-200 rounded-2xl focus:ring-2 focus:ring-[#1E3A8A] focus:border-transparent outline-none transition-all"
                required
              />
            </div>

            <div class="relative">
              <input
                type="text"
                name="codigoPostal"
                placeholder="Código Postal (Barcelona)"
                class="w-full px-5 py-4 bg-gray-50 border border-gray-200 rounded-2xl focus:ring-2 focus:ring-[#1E3A8A] focus:border-transparent outline-none transition-all"
                required
              />
            </div>

            <div class="relative">
              <select
                name="problema"
                class="w-full px-5 py-4 bg-gray-50 border border-gray-200 rounded-2xl focus:ring-2 focus:ring-[#1E3A8A] focus:border-transparent outline-none transition-all appearance-none text-gray-400 focus:text-gray-900"
                required
              >
                <option value="">¿Cuál es tu problema?</option>
                <option value="frio">Paredes frías / Pérdida de calor</option>
                <option value="moho">Moho y humedad</option>
                <option value="factura">Factura calefacción alta</option>
                <option value="sate">No puedo hacer SATE exterior</option>
              </select>
              <div
                class="absolute right-5 top-1/2 -translate-y-1/2 pointer-events-none text-gray-400"
              >
                <svg
                  width="16"
                  height="16"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  stroke-linecap="round"
                  stroke-linejoin="round"><path d="m6 9 6 6 6-6"></path></svg
                >
              </div>
            </div>

            <button
              type="submit"
              class="w-full py-5 bg-[#F97316] hover:bg-[#EA580C] text-white font-black text-xl rounded-2xl shadow-xl shadow-[#F97316]/30 transition-all transform hover:scale-[1.02] active:scale-[0.98] mt-4 uppercase tracking-tight"
            >
              Ver Cuánto Me Cuesta →
            </button>

            <div
              class="flex items-center justify-center gap-2 pt-4 text-xs font-semibold text-gray-400"
            >
              <svg
                width="14"
                height="14"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
                ><rect x="3" y="11" width="18" height="11" rx="2" ry="2"
                ></rect><path d="M7 11V7a5 5 0 0 1 10 0v4"></path></svg
              >
              <span>Sin compromiso · Respuesta en 24h · Sin spam</span>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>

  <!-- Decorative Gradient Blobs -->
  <div
    class="absolute -top-24 -left-24 w-96 h-96 bg-[#1D4ED8] rounded-full blur-[160px] opacity-20 pointer-events-none"
  >
  </div>
  <div
    class="absolute bottom-0 right-0 w-[500px] h-[500px] bg-[#1E3A8A] rounded-full blur-[200px] opacity-30 pointer-events-none"
  >
  </div>
</section>

<style>
  .hero-section {
    min-height: 85vh;
    display: flex;
    align-items: center;
  }

  /* Animations */
  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  @keyframes fadeInDown {
    from {
      opacity: 0;
      transform: translateY(-20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  @keyframes fadeInRight {
    from {
      opacity: 0;
      transform: translateX(30px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  /* Animations */
  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }
  @keyframes fadeInUp {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }
  @keyframes fadeInDown {
    from { opacity: 0; transform: translateY(-20px); }
    to { opacity: 1; transform: translateY(0); }
  }
  @keyframes fadeInRight {
    from { opacity: 0; transform: translateX(30px); }
    to { opacity: 1; transform: translateX(0); }
  }

  .animate-fade-in {
    animation: fadeIn 1s ease-out forwards;
  }
  .animate-fade-in-up {
    opacity: 0;
    animation: fadeInUp 0.8s ease-out forwards;
  }
  .animate-fade-in-down {
    animation: fadeInDown 0.8s ease-out forwards;
  }
  .animate-fade-in-right {
    opacity: 0;
    animation: fadeInRight 1s ease-out forwards;
  }

  .delay-200 { animation-delay: 0.2s; }
  .delay-300 { animation-delay: 0.3s; }
  .delay-400 { animation-delay: 0.4s; }
</style>
